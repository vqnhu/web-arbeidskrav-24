DT.onReadyIdle(()=>{const preview=document.querySelector('.js-item-preview');if(!preview)return;const PADDING_OFFSET=10;let currentOffset=PADDING_OFFSET;const updateHeaderOffset=()=>{let headerOffset=DT.getHeaderOffset()??0;headerOffset+=PADDING_OFFSET;if(currentOffset===headerOffset)return;preview.style.setProperty('--preview-top',`${headerOffset}px`);currentOffset=headerOffset};window.addEventListener('resize',DT.debounce(updateHeaderOffset,200));updateHeaderOffset()})